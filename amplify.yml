version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - |
          printf "DATABASE_URL=%s\nPOSTGRES_URL_NON_POOLING=%s\nAUTH_SECRET=%s\nDATABASE_URL=%s\n" "$DATABASE_URL" "$POSTGRES_URL_NON_POOLING" "$AUTH_SECRET" "$DATABASE_URL" > .env.production || true
        - cat .env.production || true
        - npx prisma generate
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
